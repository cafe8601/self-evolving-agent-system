#!/bin/bash
# ═══════════════════════════════════════════════════════════════════
# ecommerce-full-pipeline.sh
# E-commerce 실시간 주문 시스템 - 전체 파이프라인
#
# 사용법: ./scripts/ecommerce-full-pipeline.sh
# ═══════════════════════════════════════════════════════════════════

set -e  # 에러 발생 시 중단

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_DIR"

echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║      E-commerce 실시간 주문 시스템 - 자가 진화 파이프라인      ║"
echo "╚═══════════════════════════════════════════════════════════════╝"

# ─────────────────────────────────────────────────────────────────
# 0. Brain 상태 확인
# ─────────────────────────────────────────────────────────────────
echo ""
echo "📚 [BRAIN CHECK] 현재 학습된 패턴 확인..."
cat .opencode/brain/project_brain.yaml | grep -A2 "learned_patterns:" || echo "패턴 없음 - 빈 Brain에서 시작"

# ─────────────────────────────────────────────────────────────────
# 1. 인증 시스템
# ─────────────────────────────────────────────────────────────────
echo ""
echo "🔐 [FEATURE 1/5] JWT 인증 시스템..."
echo "JWT 기반 사용자 인증. 소셜 로그인(Google, Kakao), httpOnly 쿠키, Refresh Token Rotation" | md .mdflow/evolve.claude.md

# ─────────────────────────────────────────────────────────────────
# 2. 상품 카탈로그
# ─────────────────────────────────────────────────────────────────
echo ""
echo "📦 [FEATURE 2/5] 상품 카탈로그 API..."
echo "상품 CRUD API. Prisma ORM, 카테고리 필터, Cursor 페이지네이션, S3 이미지" | md .mdflow/evolve.claude.md

# ─────────────────────────────────────────────────────────────────
# 3. 장바구니
# ─────────────────────────────────────────────────────────────────
echo ""
echo "🛒 [FEATURE 3/5] 장바구니 시스템..."
echo "장바구니 기능. 비회원 localStorage + 로그인 병합, 재고 실시간 검증" | md .mdflow/evolve.claude.md

# ─────────────────────────────────────────────────────────────────
# 4. 실시간 주문
# ─────────────────────────────────────────────────────────────────
echo ""
echo "📋 [FEATURE 4/5] 실시간 주문 처리..."
echo "실시간 주문 시스템. WebSocket 상태 업데이트, Toss Payments, 주문 상태 머신" | md .mdflow/evolve.claude.md

# ─────────────────────────────────────────────────────────────────
# 5. 재고 동기화
# ─────────────────────────────────────────────────────────────────
echo ""
echo "📊 [FEATURE 5/5] 재고 동기화..."
echo "실시간 재고 관리. Optimistic Locking, 재고 부족 알림, 입고 예정 연동" | md .mdflow/evolve.claude.md

# ─────────────────────────────────────────────────────────────────
# 6. 보안 리뷰
# ─────────────────────────────────────────────────────────────────
echo ""
echo "🔒 [REVIEW] 전체 보안 점검..."
echo "전체 코드 보안 리뷰: OWASP Top 10, SQL Injection, XSS, CSRF 점검" | md .mdflow/review.claude.md

# ─────────────────────────────────────────────────────────────────
# 7. Brain 최종 상태
# ─────────────────────────────────────────────────────────────────
echo ""
echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║                    📚 최종 Brain 상태                          ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
npm run brain:sync 2>/dev/null || true
cat .opencode/brain/project_brain.yaml

echo ""
echo "✅ E-commerce 파이프라인 완료!"
echo "   - 학습된 패턴: $(grep -c "id: \"LP-" .opencode/brain/project_brain.yaml 2>/dev/null || echo 0)개"
